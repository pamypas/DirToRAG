class ExampleAgent:
    """
    Пример "пустого" агента.

    Сейчас он ничего не добавляет в контекст (build_context всегда
    возвращает пустую строку), но показывает минимальный интерфейс
    агента, который можно расширять в будущем.
    """

    def __init__(self) -> None:
        # сюда позже можно добавить инициализацию ресурсов
        pass

    def build_context(self, user_message: str) -> str:
        """
        Принимает пользовательское сообщение и возвращает строку
        с дополнительным контекстом для LLM.

        В этом примере всегда возвращает пустую строку и никак
        не влияет на итоговый контекст.
        """
        return ""
